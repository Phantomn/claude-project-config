# Pentest Session: heap_note
Date: 2026-03-10
Domain: Binary-Linux
Scope: CTF binary — UAF 취약점 분석 및 익스플로잇

## Binary 분석 상태
대상 유형: 로컬 앱
Architecture: x86-64
Stripped: N | Linking: dynamic

## 보호기법 + 공격 경로 가설
- Full RELRO / Canary / NX / PIE / SHSTK+IBT(CET)
- glibc: 2.39 (hook 없음, safe-linking 적용)
- seccomp: read/write/open/openat/mprotect 허용, execve 차단
- 가설: UAF → heap leak → libc leak → tcache poison → 임의 쓰기 → ORW 쉘코드

## 노트 구조체 (malloc 0x28)
```
+0x00~0x17 : title (24 bytes, read 최대 0x18)
+0x18      : size  (QWORD)
+0x20      : *content (QWORD)
```

## 발견사항
| ID   | 위치        | 유형               | 심각도 | RCA 상태     |
|------|-------------|--------------------|--------|--------------|
| F-01 | delete_note | UAF (notes[] 미null화) | High | 확정         |

### F-01 근본 원인
```c
free(*(void **)(notes[v1] + 32));  // content free
free((void *)notes[v1]);           // struct free
// notes[v1] = NULL; ← 누락!
```
- view_note / edit_note / delete_note 모두 `if (notes[v1])` 검사 후 진행
- free 후 포인터 잔존 → UAF read (view) / UAF write (edit) / double-free (delete) 모두 가능

## 공격 경로 (glibc 2.39)
```
Step 1. heap leak
  create(size=0x28) → delete → view → tcache fd (safe-linked) 읽기
  fd XOR (heap >> 12) 역산 → heap base 확보

Step 2. libc leak
  create(size=0x420) → delete → (tcache 소진 후) unsorted bin 진입
  view → bk = main_arena+offset → libc base 확보

Step 3. tcache poison
  heap base로 safe-linking XOR → fake next chunk 삽입
  malloc → 임의 주소 할당

Step 4. 임의 쓰기 대상
  hook 없음 → FSOP(_IO_2_1_stdout_) 또는 __exit_funcs 덮어쓰기 검토

Step 5. ORW 쉘코드 (endbr64 prefix 필수, CET IBT 대응)
  open("flag.txt") → read(fd, buf) → write(1, buf, len)
```

## 함수 분석 현황
| 함수명       | 주소   | 상태   | 발견사항              | 동적검증필요 |
|--------------|--------|--------|-----------------------|-------------|
| delete_note  | 0x19b7 | 완료   | UAF (미null화)        | N           |
| create_note  | 0x1504 | 완료   | malloc 0x28+N, 정상   | N           |
| edit_note    | 0x189b | 완료   | UAF write 경로 확인   | N           |
| view_note    | 0x173c | 완료   | UAF read 경로 확인    | N           |
| read_int     | 0x1494 | 완료   | 안전                  | N           |
| setup_seccomp| 0x1289 | 완료   | ORW 허용, execve 차단 | N           |

## 다음 세션 우선순위
1. PoC 작성: Step 1 heap leak (tcache fd safe-link 복호화)
2. PoC 작성: Step 2 libc leak (unsorted bin)
3. 임의 쓰기 대상 결정 (FSOP vs __exit_funcs)
4. ORW 쉘코드 완성 (endbr64 prefix)

## DEAD END
- (없음)

## 현재 가설
- 가설: UAF → tcache poison → 임의 쓰기 → ORW 쉘코드
- 검증 방법: pwntools PoC 작성, 로컬 실행 확인
- 폐기 조건: tcache poison 실패 시 → House of Botcake 또는 FSOP 직접 전환
